version: 1.0.8

endpoints:
  custom:
    # ── Ollama direct (unchanged) ────────────────────────────
    - name: "Ollama"
      apiKey: "ollama"
      baseURL: "http://ollama:11434/v1"
      models:
        default: ["mistral"]   # fallback if /tags fails
        fetch:   true          # Ollama exposes its own model list
      titleConvo: true
      titleModel: "current_model"
      summarize:  false
      modelDisplayLabel: "Ollama"

    # ── LangGraph proxy (one provider, many models) ─────────
    - name: "LangGraph"
      apiKey: "proxy"                # dummy; proxy ignores it
      baseURL: "http://langchain:8082"   # LibreChat will append /v1/…
      directEndpoint: false

      models:
        default: ["openchat"]        # placeholder (required field)
        fetch:   true                # LibreChat 👉 GET /v1/models

      titleConvo: true
      titleModel: "current_model"
      summarize:  false
      modelDisplayLabel: "LangGraph"
